[build]
builder = "RAILPACK"
buildCommand = "npm ci && npm --workspace apps/agent run build"

[deploy]
startCommand = "npm --workspace apps/agent run start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
